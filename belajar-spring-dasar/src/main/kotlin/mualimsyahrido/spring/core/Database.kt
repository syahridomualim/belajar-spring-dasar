package mualimsyahrido.spring.core

import java.util.logging.Logger

object Database {

    private val  log:  Logger = Logger.getLogger(this.toString())
    init {
        Log.log.info("Create database")
    }
}